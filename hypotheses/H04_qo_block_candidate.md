# H04 — EVA `qo` as an explicit block candidate

## Observation

In the complete uploaded RF1b-EVA file:

- literal q: 5435
- token-initial q: 5396
- token-initial literal qo: 5258

Thus 97.44% of token-initial q tokens begin with `qo`.

## Competing models

1. `q | o`: independent units with a very strong transition constraint.
2. `qo`: one phonographic/motoric unit.
3. `q(o...)`: q is an operator/modifier selecting an o-initial class.

## Falsification-oriented test

Compare predictive/compression performance of these segmentations and inspect whether
the distribution following `qo` behaves as a coherent unit across hands, Currier states,
and sections.

No sound value is assigned.
