# Phase 21 — transcription and palaeography robustness gate

This checkpoint does not fit a new Voynich model and does not score an
alternative transcription. It supersedes the Phase-20 authorization for a
line-final/copy-modify/vocabulary-innovation generator ladder.

The only authorized scientific gate is a cross-transcription and
image-adjudicated robustness test of four existing surface claims. Source
payload acquisition, exact hashes, harmonization, TRAIN-only tolerance
calibration and an analysis-code freeze are still required before any
challenger VALIDATION metric may be computed.

Run the checkpoint validator with:

```text
python3 phase21_transcription_robustness/validate_phase21_checkpoint.py --repo .
```

No Phase-21 file authorizes final-test use, hypothesis ranking, a new language
attack, a generator ladder, a capacity re-estimation or a larger latent-state
model.

